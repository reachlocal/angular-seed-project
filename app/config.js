/**
 * Configuration settings for the client-app
 */
var rlConfig = {
    // Where is the cpi-gateway?
    gatewayBaseUrl: "http://localhost:8001",
    defaultLocale: 'en',
    locales: ['en', 'pt']
};